# Адрес API админки. Если пусто, сайт работает на данных из src/data
# и заявки складываются в localStorage.
VITE_API_URL=

# Куда слать заявки, если API админки не используется (например, PHP-скрипт)
VITE_LEAD_ENDPOINT=
